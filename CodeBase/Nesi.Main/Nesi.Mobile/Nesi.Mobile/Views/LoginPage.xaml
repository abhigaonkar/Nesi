<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Nesi.Mobile"
			 xmlns:local1="clr-namespace:Nesi.Mobile.ViewModels"
			 x:Class="Nesi.Mobile.MainPage">
	<ContentPage.BindingContext>
		<local1:LoginPageViewModel/>
	</ContentPage.BindingContext>

	<ContentPage.Content>
		<Grid Margin="16">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<StackLayout Grid.Row="0">
				<Image Source="nesi.png"/>
				<Label Text="Please enter your credentials" />
				<Entry Text="{Binding Username}"
                       Placeholder="Username" />
				<Entry Text="{Binding Password}"
                       Placeholder="Password"
                       IsPassword="True" />
				<Label Text="Invalid credentials entered" 
                       IsVisible="{Binding AreCredentialsInvalid}" 
                       TextColor="Red" />
				<ActivityIndicator IsRunning="{Binding IsBusy}"/>
				<Button Grid.Row="1" Text="Login" Command="{Binding LoginCommand}" />
				
			</StackLayout>
		
		</Grid>
	</ContentPage.Content>

</ContentPage>
